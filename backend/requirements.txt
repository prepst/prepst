fastapi==0.115.6
uvicorn[standard]==0.34.0
supabase==2.9.0
python-dotenv>=0.21.0,<0.22.0
pydantic==2.9.2
httpx==0.27.2
pydantic-settings==2.6.0
email-validator==2.3.0
openai==1.66.1
tabulate==0.9.0
python-multipart==0.0.20
# Note: manim and manim-voiceover are not included as they require system dependencies
# (pangocairo, cairo, ffmpeg) that are not available in Vercel's serverless environment.
# Install manim-voiceover on Railway or other Docker-supported platform for manim functionality.
# For local development: pip install manim-voiceover
